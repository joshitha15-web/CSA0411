#include <stdio.h>
#include <stdlib.h>

int compare(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

int main() {
    int queue[] = {98, 183, 37, 122, 14, 124, 65, 67};
    int size = 8, head = 53, seek = 0;
    qsort(queue, size, sizeof(int), compare);

    printf("C-SCAN Disk Scheduling:\n");
    for (int i = 0; i < size; i++) {
        if (queue[i] >= head) {
           
