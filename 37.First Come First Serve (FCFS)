#include <stdio.h>

int main() {
    int queue[] = {98, 183, 37, 122, 14, 124, 65, 67};
    int head = 53, seek = 0;

    for (int i = 0; i < 8; i++) {
        seek += abs(queue[i] - head);
        head = queue[i];
    }

    printf("Total Seek Time (FCFS): %d\n", seek);
    return 0;
}
